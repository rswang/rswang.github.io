# Recipe photos

Put recipe photos here, then reference them from `recipes.js`.

Tips:
- Square-ish photos look best as gallery tiles (they're cropped to a square).
- Name files to match the recipe id, e.g. `banana-bread.jpg`.
- Keep file sizes reasonable (a few hundred KB) so pages load fast.
