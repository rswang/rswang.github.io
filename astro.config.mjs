// @ts-check
import { defineConfig } from 'astro/config';

// User pages site served at the domain root (https://rswang.github.io),
// so no `base` is needed.
export default defineConfig({
  site: 'https://rswang.github.io',
});
